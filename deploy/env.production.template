# VisualDesign Admin Panel — Environment Variables for Production Server
# Copy this file to /home/admin.DOMAIN/public_html/.env.local on the server
# Replace all <placeholder> values

NEXT_PUBLIC_SUPABASE_URL=https://gwankhxcbkrtgxopbxwd.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_NkNwKuVE-AyGgyxKB6zpmQ_b-HdjWOA

# CyberPanel REST API — use localhost because app runs on same server
CYBERPANEL_URL=https://localhost:8090/api
CYBERPANEL_PASS=<your_cyberpanel_admin_password>
CYBERPANEL_USER=admin

# SSH — not needed when CYBERPANEL_USE_LOCAL_EXEC=true
CYBERPANEL_IP=127.0.0.1
CYBERPANEL_SSH_PORT=22
CYBERPANEL_SSH_USER=root

# KEY SETTING: set true so all routes use child_process.exec instead of SSH
# This works because the Next.js app runs on the SAME server as CyberPanel
CYBERPANEL_USE_LOCAL_EXEC=true
