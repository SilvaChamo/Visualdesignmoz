# Autenticação cliente → Brevo SMTP relay (porta 587)
brevo_client:
  driver = plaintext
  public_name = LOGIN
  client_send = : ${readfile{/etc/exim.brevo.user}{}} : ${readfile{/etc/exim.brevo.pass}{}}
