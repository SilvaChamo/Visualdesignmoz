⚡ CONFIGURAÇÃO RÁPIDA - Copie e cole no terminal

1️⃣ LIGAR AO SERVIDOR:
ssh ADMIN@37.27.17.25
(password: Meckito#77?*)

2️⃣ EXECUTAR COMANDOS NO SERVIDOR:
sudo bash -c '
# Abrir portas
ufw allow 587/tcp && ufw allow 465/tcp && ufw reload

# Configurar Postfix
postconf -e "inet_interfaces = all"
postconf -e "smtpd_sasl_auth_enable = yes"
postconf -e "smtpd_recipient_restrictions = permit_mynetworks, permit_sasl_authenticated, reject_unauth_destination"

# Adicionar submission na porta 587
cat >> /etc/postfix/master.cf << "EOF"

submission inet n       -       -       -       -       smtpd
  -o smtpd_tls_security_level=encrypt
  -o smtpd_sasl_auth_enable=yes
  -o smtpd_client_restrictions=permit_sasl_authenticated,reject
EOF

# Restart
systemctl restart postfix

# Verificar
netstat -tlnp | grep :587
echo "✅ Configuracao concluida!"
'

3️⃣ TESTAR:
telnet 37.27.17.25 587

4️⃣ NO SITE: Enviar email de teste
